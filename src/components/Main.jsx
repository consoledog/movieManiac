export default function Main({children}) {
    return (
        <main className="bg-primary mt-[2.4rem] h-[calc(100vh-7.2rem-7.2rem)] flex gap-[2.4rem] justify-center">
            {children}
        </main>
    )
}